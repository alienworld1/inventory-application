extends layout 

block content 
  h1= title

  form(method="post")
    section 
      label(for='name') Name:
      input#name(type="text" placeholder='Fruits, Dairy Products etc.' name='name' required value=(category===undefined ? '' : category.name)) 
      label(for='description') Description: 
      input#description(type='text' name='description' required minlength=5 value=(category===undefined ? '' : category.description))
    button(type="submit") Submit

  if errors
    ul 
      for error in errors 
        li!= error.msg